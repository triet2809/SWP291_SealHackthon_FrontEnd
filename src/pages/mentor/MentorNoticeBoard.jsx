import React, { useEffect, useState } from 'react';
import { Alert, Button, Card, Col, Form, Row, Spinner } from 'react-bootstrap';
import { Clock } from 'lucide-react';
import { createNotice, getNotices } from '../../api/hackathonApi';
import { getInitials } from '../../utils/authUser';
import styles from './MentorNoticeBoard.module.css';

const items = (data) => data?.content || data || [];

const MentorNoticeBoard = () => {
  const [notices, setNotices] = useState([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState('');
  const [showForm, setShowForm] = useState(false);
  const [form, setForm] = useState({ title: '', content: '', priority: 'normal', targetRole: 'team_member' });
  const load = async () => { const data = await getNotices({ targetRole: 'mentor', size: 50 }); setNotices(items(data)); };
  useEffect(() => { (async () => { try { await load(); } catch (e) { setError(e.message || 'Cannot load notices'); } finally { setLoading(false); } })(); }, []);
  const post = async (e) => { e.preventDefault(); setError(''); try { await createNotice(form); setForm({ title: '', content: '', priority: 'normal', targetRole: 'team_member' }); setShowForm(false); const data = await getNotices({ targetRole: 'mentor', size: 50 }); setNotices(items(data)); } catch (err) { setError(err.message || 'Failed to post notice'); } };
  const getAvatarClass = (role) => role === 'judge' ? styles.avatarJudge : role === 'mentor' ? styles.avatarMentor : styles.avatarAdmin;
  const getRoleClass = (role) => role === 'judge' ? styles.roleJudge : role === 'mentor' ? styles.roleMentor : styles.roleAdmin;
  if (loading) return <div className="py-4 text-center"><Spinner size="sm" className="me-2" />Loading notices...</div>;
  return <div className="py-2"><div className={styles.pageHeader}><div className="d-flex justify-content-between align-items-end"><div><h1 className={styles.pageTitle}>Notice Board</h1><div className={styles.pageSubtitle}>Post updates to your teams and view global announcements</div></div>{!showForm && <Button className={styles.postBtn} onClick={() => setShowForm(true)}>+ Create Announcement</Button>}</div></div>{error && <Alert variant="danger">{error}</Alert>}{showForm && <Card className={styles.postCard}><Card.Body className="p-4"><div className="d-flex justify-content-between align-items-center mb-4"><h5 style={{ fontWeight: 600, margin: 0 }}>Create Announcement</h5><Button variant="link" className="text-muted p-0 text-decoration-none" onClick={() => setShowForm(false)}>Cancel</Button></div><Form onSubmit={post}><Row className="mb-3"><Col md={8}><Form.Group><Form.Label className={styles.formLabel}>Title</Form.Label><Form.Control type="text" className={styles.formControl} value={form.title} onChange={(e) => setForm((f) => ({ ...f, title: e.target.value }))} required /></Form.Group></Col><Col md={4}><Form.Group><Form.Label className={styles.formLabel}>To:</Form.Label><Form.Select className={styles.formControl} value={form.targetRole} onChange={(e) => setForm((f) => ({ ...f, targetRole: e.target.value }))}><option value="team_member">Teams</option><option value="mentor">Mentors</option><option value="judge">Judges</option><option value="">Global</option></Form.Select></Form.Group></Col></Row><Form.Group className="mb-3"><Form.Label className={styles.formLabel}>Message Content</Form.Label><Form.Control as="textarea" rows={4} className={styles.formControl} value={form.content} onChange={(e) => setForm((f) => ({ ...f, content: e.target.value }))} required /></Form.Group><div className="d-flex justify-content-between align-items-center"><Form.Check type="checkbox" id="important-flag" label="Flag as Important" checked={form.priority === 'high'} onChange={(e) => setForm((f) => ({ ...f, priority: e.target.checked ? 'high' : 'normal' }))} className={styles.formLabel} style={{ marginBottom: 0 }} /><Button type="submit" className={styles.postBtn}>Post Notice</Button></div></Form></Card.Body></Card>}<div className={styles.noticeList}>{notices.length === 0 ? <Card className={styles.noticeCard}><Card.Body className="p-4 text-muted">No notices.</Card.Body></Card> : notices.map((notice) => <Card key={notice.id} className={`${styles.noticeCard} ${notice.priority === 'high' ? styles.priorityHigh : styles.priorityNormal}`}><Card.Body className="p-4"><div className={styles.cardHeader}><div className={styles.authorInfo}><div className={`${styles.avatar} ${getAvatarClass(notice.targetRole)}`}>{getInitials(notice.authorName || notice.authorEmail)}</div><div className={styles.authorDetails}><span className={styles.authorName}>{notice.authorName || notice.authorEmail}</span><span className={`${styles.authorRole} ${getRoleClass(notice.targetRole)}`}>{notice.targetRole || 'global'}</span></div></div><div className={styles.noticeDate}><Clock size={12} />{notice.createdAt ? new Date(notice.createdAt).toLocaleString() : ''}</div></div><h5 className={styles.noticeTitle}>{notice.title}{notice.priority === 'high' && <span className={styles.importantIndicator}>Important</span>}</h5><p className={styles.noticeContent}>{notice.content}</p></Card.Body></Card>)}</div></div>;
};

export default MentorNoticeBoard;
